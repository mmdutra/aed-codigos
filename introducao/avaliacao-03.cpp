
#include<iostream>

using namespace std;

void show(int values[], int length) {
    for(unsigned i = 0; i < length; ++i)
        cout << values[i] << "\n";
}

int[] invert(int values[], int length) {
}

int main() {
    int values[];
    int length = sizeof(values) / sizeof(values[0]);
}
